# KeyPale 配布用ビルド手順

Electron アプリを **全プラットフォーム向け・ポータブル版**（インストール不要）として配布する手順です。

---

## 1. 前提条件

- **Node.js** がインストールされていること（開発時と同じ環境でOK）
- プロジェクトの **依存関係がインストール済み** であること（`npm install` 済み）

---

## 2. コマンドの実行について

この手順の **PowerShell コマンドは、Cursor のターミナルからそのまま実行して問題ありません**。  
Cursor の「ターミナル」は通常の PowerShell／コマンドプロンプトと同じなので、プロジェクトフォルダを開いた状態で `npm install` や `npm run dist` を実行できます。

---

## 3. electron-builder のインストール

初回のみ、ビルド用ツールを入れます。

```powershell
npm install
```

`package.json` の `devDependencies` に `electron-builder` が含まれているので、上記で一緒にインストールされます。

---

## 4. ビルドの実行

### 全プラットフォーム向け（ポータブル版のみ）

Windows / macOS / Linux 向けに、いずれも **インストール不要のポータブル形式** でビルドします。

```powershell
npm run dist
```

現在の OS 用だけビルドしたい場合（例: Windows だけ）：

```powershell
npm run dist:win
```

### 中身だけ確認したい場合（pack）

インストーラーや配布用ファイルを作らず、`dist` フォルダ内に「そのまま動かせるアプリのファイル一式」だけを出力します。動作確認用です。

```powershell
npm run pack
```

---

## 5. 出力先

ビルドが完了すると、プロジェクト直下の **`dist`** フォルダに、**現在の OS 用**のファイルができます。

| プラットフォーム | 出力ファイル | 説明 |
|------------------|--------------|------|
| **Windows** | `KeyPale 1.0.0.exe` | ポータブル版。この exe 1つで起動できます。 |
| **macOS** | `KeyPale 1.0.0-mac.zip` | .app を格納した zip。解凍してアプリを起動します。 |
| **Linux** | `KeyPale 1.0.0.AppImage` | AppImage。1ファイルで起動できます。 |

※ バージョン番号（`1.0.0`）は `package.json` の `version` に合わせて変わります。  
※ macOS / Linux 用は、それぞれの OS 上でビルドする必要があります（クロスビルドは別設定が必要です）。

---

## 5.5 CI で Mac 用をビルドする（Windows だけの開発環境の場合）

**Windows では Mac 用 .app をビルドできません。** 代わりに **GitHub Actions** で「Mac の仮想マシン」上だけ Mac 用をビルドできます。

### 手順の流れ

1. このリポジトリを **GitHub にプッシュ**する（Git の初期化・リモート追加がまだなら行う）。
2. リポジトリに **`.github/workflows/build-mac.yml`** を置く（本プロジェクトに同梱済み）。
3. GitHub の **Actions** タブで **「Build for macOS」** ワークフローを **手動実行**（Run workflow）する。
4. 完了後、**Artifacts** から **KeyPale-macOS** をダウンロードすると、中に Mac 用の zip（.app 入り）が入っています。

### ワークフローの動き

| ステップ | 内容 |
|----------|------|
| 実行環境 | `macos-latest`（GitHub が用意する Mac のランナー） |
| Checkout | リポジトリのコードを取得 |
| Node.js | Node 20 をセットアップ、`npm` のキャッシュを使用 |
| 依存関係 | `npm ci` でインストール |
| ビルド | `npm run dist:mac`（`electron-builder --mac`）で Mac 用のみビルド |
| アップロード | 生成された zip / .app を **Artifacts** に「KeyPale-macOS」として保存 |

### トリガー

- **手動**: Actions タブ → 「Build for macOS」→ **Run workflow** でいつでも実行可能。
- **自動**（任意）: `main` または `master` に push したときにも実行されます（`.md` だけの変更は除外済み）。

### 注意

- **リポジトリが GitHub にない**場合は、まず GitHub で新規リポジトリを作り、`git remote add origin ...` でプッシュしてください。
- 初回は Mac 用 Electron のダウンロードで数分かかることがあります。
- 成果物は **署名なし** のため、Mac で初回起動時に「開発元を確認できない」と出る場合は、システム環境設定の「セキュリティとプライバシー」から実行を許可する必要があります。

---

## 6. バージョンや名前の変更

- **アプリのバージョン**: `package.json` の `"version": "1.0.0"` を変更してから `npm run dist` を実行すると、出力ファイル名のバージョン部分が変わります。
- **アプリ名**: `package.json` の `"productName": "KeyPale"` を変更すると、exe やアプリの名前が変わります。

---

## 7. アイコンについて

- ビルドでは **`favicon.ico`** を exe／アプリのアイコンとして使用しています（ブラウザのファビコンと同じファイルでOK）。
- **サイズは 256×256 ピクセル以上** である必要があります（electron-builder の仕様）。小さいと「must be at least 256x256」でビルドが止まります。512×512 などにしておくと安心です。
- アイコンを差し替えたあとは、もう一度 `npm run dist` でビルドし直してください。

---

## 8. トラブルシューティング

- **「electron-builder が見つからない」**: プロジェクトフォルダで `npm install` を実行してください。
- **「favicon.ico must be at least 256x256」**: ビルド用アイコンは 256×256 以上が必要です。`favicon.ico` を 256×256 以上（推奨 512×512）の .ico に差し替えてください。
- **「Cannot create symbolic link」／winCodeSign で失敗する**: コード署名用ツールの解凍で、Windows の権限不足により失敗することがあります。現在は **コード署名を無効**（`signAndEditExecutable: false`）にしてあるため、このエラーは出ずにビルド完了する想定です。
- **ビルドが重い／時間がかかる**: 初回は Electron のバイナリを取得するため時間がかかることがあります。
- **ウイルス対策ソフトが反応する**: 未署名の exe は検知されることがあります。必要に応じてコード署名証明書の利用を検討してください。

---

## 9. 配布時の注意

- **ポータブル版** はインストール不要なので、exe（Windows）や zip / AppImage（Mac / Linux）をそのまま配布できます。設定やデータの保存先は、起動したユーザーのアプリデータフォルダなどに保存されます（Electron の仕様）。
- ユーザーには「ファイルをダウンロードして実行（Windows）」「zip を解凍してアプリを起動（Mac）」「AppImage に実行権限を付けて起動（Linux）」と案内するとよいです。

---

## 10. 配布時に同梱するもの

配布用の zip やダウンロードページを用意する場合の目安です。

| 内容 | 説明 |
|------|------|
| **exe（または Mac/Linux 用ファイル）** | `dist` フォルダのビルド成果物。必須。 |
| **LICENSE** | 本アプリの利用条件。exe 内にも入っていますが、**配布用 zip などに同じファイルを添えると「受け取った人がそのまま開いて読める」ので推奨。** |
| **THIRD_PARTY_LICENSES.md** | CodeMirror・Golden Layout などの利用ライブラリのライセンス表記。**MIT の条件により配布時に含める必要があります。** |
| **README や使い方** | 任意。初回ユーザー向けの説明があると親切です。 |

- **help.html** はビルド時に exe 内に含まれるため、アプリ内の「ヘルプを開く」で開けます。別ファイルとして配布する必要はありません。
- 配布形態が「exe 1つだけ」の場合、ライセンス文は exe 内に含まれています。ダウンロードページやリリースノートに「CodeMirror / Golden Layout を利用しています。各ライセンスは THIRD_PARTY_LICENSES.md を参照してください」と書いておくと安心です。

以上で、KeyPale を配布用にアプリケーション化してビルドできます。
